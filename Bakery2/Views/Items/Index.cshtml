@using Bakery.Models;

<h1>Orders List</h1>
@if (Model.Count == 0)
{
  <p>There are no orders in the list.</p>
}
<ul>
  @foreach (Order order in Model)
  {
    <li><a href='/order/@order.Id'>@order.Description</a></li>
  }
</ul>

<a href="/order/new">Add a new item.</a>

